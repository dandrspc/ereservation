spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/ereservation
    username: root
    password: toor
    driver-class-name=org: mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true
    show-sql: true
